import { PageTransition } from "@/components/console/PageTransition";

export default function PackDetailPage() {
  return (
    <PageTransition>
      <div className="p-6">
        <h1 className="text-xl font-bold">记忆包详情</h1>
        <p className="mt-2 text-sm text-[var(--text-secondary)]">记忆包详情页将在后续迭代中完善。</p>
      </div>
    </PageTransition>
  );
}
