# i18n Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add full i18n (Chinese default + English) to the entire site using next-intl, replace QIHANG branding with 铭润科技 / Mingrun Tech, and add a language switcher.

**Architecture:** Install next-intl. Restructure routes under `[locale]` segment. Extract all hardcoded text into JSON translation files organized by namespace. Replace `next/link` with locale-aware `Link` from `i18n/navigation.ts`. Add `LanguageSwitcher` to SiteHeader and TopBar.

**Tech Stack:** next-intl, Next.js App Router, TypeScript

---

## Chunk 1: Infrastructure

### Task 1: Install next-intl and create i18n config files

**Files:**
- Modify: `apps/web/package.json`
- Create: `apps/web/i18n/routing.ts`
- Create: `apps/web/i18n/request.ts`
- Create: `apps/web/i18n/navigation.ts`

- [ ] **Step 1: Install next-intl**

```bash
cd /Users/dog/Desktop/铭润/apps/web && npm install next-intl
```

- [ ] **Step 2: Create `apps/web/i18n/routing.ts`**

```ts
import { defineRouting } from "next-intl/routing";

export const routing = defineRouting({
  locales: ["zh", "en"],
  defaultLocale: "zh",
  localePrefix: "as-needed",
});
```

- [ ] **Step 3: Create `apps/web/i18n/request.ts`**

```ts
import { getRequestConfig } from "next-intl/server";
import { routing } from "./routing";

export default getRequestConfig(async ({ requestLocale }) => {
  let locale = await requestLocale;
  if (!locale || !routing.locales.includes(locale as any)) {
    locale = routing.defaultLocale;
  }

  const namespaces = [
    "common", "home", "product", "ecosystem", "demo", "pricing",
    "support", "updates", "auth", "error", "console",
    "console-projects", "console-datasets", "console-train",
    "console-models", "console-eval", "console-settings",
    "console-devices", "console-billing",
  ];
  const messages: Record<string, any> = {};
  for (const ns of namespaces) {
    messages[ns] = (await import(`../messages/${locale}/${ns}.json`)).default;
  }

  return { locale, messages };
});
```

- [ ] **Step 4: Create `apps/web/i18n/navigation.ts`**

```ts
import { createNavigation } from "next-intl/navigation";
import { routing } from "./routing";

export const { Link, redirect, usePathname, useRouter } =
  createNavigation(routing);
```

- [ ] **Step 5: Commit**

```bash
git add apps/web/package.json apps/web/package-lock.json apps/web/i18n/
git commit -m "feat(i18n): install next-intl and create i18n config files"
```

---

### Task 2: Create middleware and update next.config.js

**Files:**
- Create: `apps/web/middleware.ts`
- Modify: `apps/web/next.config.js`

- [ ] **Step 1: Create `apps/web/middleware.ts`**

```ts
import createMiddleware from "next-intl/middleware";
import { routing } from "./i18n/routing";

export default createMiddleware(routing);

export const config = {
  matcher: ["/((?!api|_next|.*\\..*).*)"],
};
```

- [ ] **Step 2: Update `apps/web/next.config.js`**

```js
const createNextIntlPlugin = require("next-intl/plugin");
const withNextIntl = createNextIntlPlugin();

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  async redirects() {
    return [
      {
        source: "/how-it-works",
        destination: "/product",
        permanent: true,
      },
      {
        source: "/docs",
        destination: "/support",
        permanent: true,
      },
      {
        source: "/contact",
        destination: "/support#contact",
        permanent: true,
      },
    ];
  },
};

module.exports = withNextIntl(nextConfig);
```

- [ ] **Step 3: Commit**

```bash
git add apps/web/middleware.ts apps/web/next.config.js
git commit -m "feat(i18n): add next-intl middleware and plugin wrapper"
```

---

### Task 3: Create all translation JSON files

**Files:**
- Create: `apps/web/messages/zh/common.json`
- Create: `apps/web/messages/zh/home.json`
- Create: `apps/web/messages/zh/product.json`
- Create: `apps/web/messages/zh/ecosystem.json`
- Create: `apps/web/messages/zh/demo.json`
- Create: `apps/web/messages/zh/pricing.json`
- Create: `apps/web/messages/zh/support.json`
- Create: `apps/web/messages/zh/updates.json`
- Create: `apps/web/messages/zh/auth.json`
- Create: `apps/web/messages/zh/error.json`
- Create: `apps/web/messages/zh/console.json`
- Create: `apps/web/messages/zh/console-projects.json`
- Create: `apps/web/messages/zh/console-datasets.json`
- Create: `apps/web/messages/zh/console-train.json`
- Create: `apps/web/messages/zh/console-models.json`
- Create: `apps/web/messages/zh/console-eval.json`
- Create: `apps/web/messages/zh/console-settings.json`
- Create: `apps/web/messages/zh/console-devices.json`
- Create: `apps/web/messages/zh/console-billing.json`
- Create: `apps/web/messages/en/` (same structure)

- [ ] **Step 1: Create `messages/zh/common.json`**

```json
{
  "brand.company": "铭润科技",
  "brand.product": "铭润科技 AI 耳机",
  "nav.product": "产品",
  "nav.ecosystem": "AI 生态",
  "nav.demo": "在线体验",
  "nav.pricing": "定价",
  "nav.support": "支持",
  "nav.getStarted": "开始使用",
  "nav.tryDemo": "体验 Demo",
  "nav.openMenu": "打开菜单",
  "nav.closeMenu": "关闭菜单",
  "lang.switch": "EN",
  "lang.current": "中",
  "footer.product": "产品",
  "footer.product.overview": "产品概览",
  "footer.product.specs": "技术规格",
  "footer.product.craftsmanship": "工艺细节",
  "footer.ecosystem": "AI 生态",
  "footer.ecosystem.platform": "平台",
  "footer.ecosystem.demo": "在线体验",
  "footer.ecosystem.pricing": "定价",
  "footer.support": "支持",
  "footer.support.docs": "文档",
  "footer.support.faq": "常见问题",
  "footer.support.contact": "联系我们",
  "footer.about": "关于",
  "footer.about.updates": "动态",
  "footer.about.email": "邮件",
  "footer.copyright": "© 2026 铭润科技 保留所有权利。"
}
```

- [ ] **Step 2: Create `messages/en/common.json`**

```json
{
  "brand.company": "Mingrun Tech",
  "brand.product": "Mingrun Tech AI Earphone",
  "nav.product": "Product",
  "nav.ecosystem": "AI Ecosystem",
  "nav.demo": "Demo",
  "nav.pricing": "Pricing",
  "nav.support": "Support",
  "nav.getStarted": "Get Started",
  "nav.tryDemo": "Try Demo",
  "nav.openMenu": "Open menu",
  "nav.closeMenu": "Close menu",
  "lang.switch": "中",
  "lang.current": "EN",
  "footer.product": "Product",
  "footer.product.overview": "Overview",
  "footer.product.specs": "Specs",
  "footer.product.craftsmanship": "Craftsmanship",
  "footer.ecosystem": "AI Ecosystem",
  "footer.ecosystem.platform": "Platform",
  "footer.ecosystem.demo": "Online Demo",
  "footer.ecosystem.pricing": "Pricing",
  "footer.support": "Support",
  "footer.support.docs": "Documentation",
  "footer.support.faq": "FAQ",
  "footer.support.contact": "Contact",
  "footer.about": "About",
  "footer.about.updates": "Updates",
  "footer.about.email": "Email",
  "footer.copyright": "© 2026 Mingrun Tech. All rights reserved."
}
```

- [ ] **Step 3: Create `messages/zh/home.json`**

```json
{
  "meta.title": "铭润科技 - 环境 AI 工作台",
  "meta.description": "离线优先的环境 AI 设备与数据工作流。",
  "hero.eyebrow": "铭润科技 / 环境 AI",
  "hero.title": "看见周围，\n理解周围。",
  "hero.body": "一枚随身佩戴的 AI 设备——看见你所处的环境，即时给出反馈。无需掏出手机，无需打开应用。",
  "hero.imageAlt": "耳机产品主图 / 3D 查看器",
  "highlights.eyebrow": "核心能力",
  "highlights.title": "三个核心能力。",
  "highlights.detail0.label": "随身佩戴",
  "highlights.detail0.body": "胸前相机 + 圆盘盒 + 无线耳机，三件一体。",
  "highlights.detail1.label": "即时反馈",
  "highlights.detail1.body": "拍下画面，几秒内通过耳机收到 AI 语音回答。",
  "highlights.detail2.label": "隐私可控",
  "highlights.detail2.body": "你决定何时开始、何时停止，状态灯始终可见。",
  "ecosystem.eyebrow": "AI 生态",
  "ecosystem.title": "不只是硬件。",
  "ecosystem.body": "从数据采集到模型训练，从个性化调优到云端部署——完整的 AI 工作台，让设备越用越聪明。",
  "ecosystem.detail0.label": "数据工作台",
  "ecosystem.detail0.body": "上传、标注、版本管理，一站完成。",
  "ecosystem.detail1.label": "模型训练",
  "ecosystem.detail1.body": "一键启动训练任务，实时查看曲线和日志。",
  "ecosystem.detail2.label": "个性化部署",
  "ecosystem.detail2.body": "模型发布到设备，离线也能推理。",
  "ecosystem.learnMore": "了解更多",
  "ecosystem.imageAlt": "AI 生态示意图",
  "craft.eyebrow": "工艺",
  "craft.title": "每一处细节。",
  "craft.body": "112° 精密阻尼铰链、磁吸分离相机、触感确定的开合——工业设计为日常使用而生。",
  "cta.title": "准备好了？",
  "cta.body": "体验在线 Demo，或了解完整产品。",
  "cta.demo": "体验 Demo",
  "cta.product": "了解产品"
}
```

- [ ] **Step 4: Create `messages/en/home.json`**

```json
{
  "meta.title": "Mingrun Tech - Environment AI Workbench",
  "meta.description": "Offline-first environment AI device and data workflow.",
  "hero.eyebrow": "Mingrun Tech / Environment AI",
  "hero.title": "See your surroundings.\nUnderstand them.",
  "hero.body": "A wearable AI device that sees your environment and responds in real time. No phone. No app.",
  "hero.imageAlt": "Earphone hero shot / 3D viewer",
  "highlights.eyebrow": "Core Capabilities",
  "highlights.title": "Three core capabilities.",
  "highlights.detail0.label": "Wearable",
  "highlights.detail0.body": "Chest camera + capsule case + wireless earbuds — three in one.",
  "highlights.detail1.label": "Instant feedback",
  "highlights.detail1.body": "Capture a scene, get an AI voice response in seconds.",
  "highlights.detail2.label": "Privacy in your hands",
  "highlights.detail2.body": "You decide when it starts and stops. Status light always visible.",
  "ecosystem.eyebrow": "AI Ecosystem",
  "ecosystem.title": "More than hardware.",
  "ecosystem.body": "From data collection to model training, from personalization to cloud deployment — a complete AI workbench that makes your device smarter over time.",
  "ecosystem.detail0.label": "Data workspace",
  "ecosystem.detail0.body": "Upload, annotate, and version your data in one place.",
  "ecosystem.detail1.label": "Model training",
  "ecosystem.detail1.body": "Launch training jobs with one click. Watch curves and logs in real time.",
  "ecosystem.detail2.label": "On-device deployment",
  "ecosystem.detail2.body": "Push models to devices. Inference works offline.",
  "ecosystem.learnMore": "Learn More",
  "ecosystem.imageAlt": "AI ecosystem illustration",
  "craft.eyebrow": "Craftsmanship",
  "craft.title": "Every last detail.",
  "craft.body": "A 112° precision-damped hinge, magnetically detachable camera, and tactile open-close — industrial design built for daily use.",
  "cta.title": "Ready?",
  "cta.body": "Try the online demo, or explore the full product.",
  "cta.demo": "Try Demo",
  "cta.product": "Explore Product"
}
```

- [ ] **Step 5: Create `messages/zh/product.json`**

```json
{
  "meta.title": "产品 - 铭润科技",
  "hero.title": "随身携带的 AI 感知系统。",
  "hero.cta": "体验产品",
  "scenes.surface.eyebrow": "Product",
  "scenes.surface.title": "随身携带的 AI 感知系统。",
  "scenes.surface.summary": "圆盘盒、无线耳机、胸前相机——三件组合，覆盖视觉采集、语音交互和触觉反馈。",
  "scenes.surface.stageLabel": "产品总览",
  "scenes.surface.stageSummary": "三件套，各司其职。",
  "scenes.surface.detail0.label": "圆盘盒",
  "scenes.surface.detail0.body": "触控面板 + 耳机充电仓 + 状态指示灯，一体成型。",
  "scenes.surface.detail1.label": "胸前相机",
  "scenes.surface.detail1.body": "8g 磁吸模块，安静待机，按下即拍。",
  "scenes.surface.detail2.label": "无线耳机",
  "scenes.surface.detail2.body": "AI 结果通过语音实时传达，解放双手和视线。",
  "scenes.surface.tags": "圆盘盒,相机,耳机",
  "scenes.geometry.eyebrow": "Structure",
  "scenes.geometry.title": "打开盒子，取出装备。",
  "scenes.geometry.summary": "112° 磁吸转轴单手开合，耳机自动弹出就位，相机磁吸分离即可佩戴。",
  "scenes.geometry.stageLabel": "结构细节",
  "scenes.geometry.stageSummary": "每一步都有触觉确认。",
  "scenes.geometry.detail0.label": "转轴开合",
  "scenes.geometry.detail0.body": "精密阻尼铰链，开合手感确定，角度可停。",
  "scenes.geometry.detail1.label": "耳机弹出",
  "scenes.geometry.detail1.body": "盒盖打开后耳机微抬，方便单手取用。",
  "scenes.geometry.detail2.label": "相机分离",
  "scenes.geometry.detail2.body": "磁吸卡扣释放，挂上胸前即进入待机。",
  "scenes.geometry.tags": "转轴,弹出,磁吸",
  "scenes.wear.eyebrow": "Daily Use",
  "scenes.wear.title": "戴上它，开始你的一天。",
  "scenes.wear.summary": "相机挂在胸前感知环境，耳机传回 AI 反馈。通勤、工作、探索——它跟着你。",
  "scenes.wear.stageLabel": "日常佩戴",
  "scenes.wear.stageSummary": "轻到忘记它的存在。",
  "scenes.wear.detail0.label": "佩戴",
  "scenes.wear.detail0.body": "相机 8g，耳机 4.5g，几乎无感。",
  "scenes.wear.detail1.label": "触发",
  "scenes.wear.detail1.body": "按下盒面按键或轻触耳机，开始环境感知。",
  "scenes.wear.detail2.label": "反馈",
  "scenes.wear.detail2.body": "识别结果 2-5 秒内通过耳机语音播报。",
  "scenes.wear.tags": "佩戴,触发,反馈",
  "scenes.engineering.eyebrow": "Engineering",
  "scenes.engineering.title": "可检视，可拆解，可量产。",
  "scenes.engineering.summary": "透视结构、材质标注、公差检查——从设计到制造的完整工程视角。",
  "scenes.engineering.stageLabel": "工程视角",
  "scenes.engineering.stageSummary": "从渲染到量产。",
  "scenes.engineering.detail0.label": "爆炸视图",
  "scenes.engineering.detail0.body": "所有零件分层展示，装配关系一目了然。",
  "scenes.engineering.detail1.label": "材质标注",
  "scenes.engineering.detail1.body": "每个部件标注材料、工艺和表面处理。",
  "scenes.engineering.detail2.label": "制造就绪",
  "scenes.engineering.detail2.body": "可直接导出工程文件，进入开模流程。",
  "scenes.engineering.tags": "爆炸图,材质,制造"
}
```

- [ ] **Step 6: Create `messages/en/product.json`**

```json
{
  "meta.title": "Product - Mingrun Tech",
  "hero.title": "A portable AI perception system.",
  "hero.cta": "Experience the product",
  "scenes.surface.eyebrow": "Product",
  "scenes.surface.title": "A portable AI perception system.",
  "scenes.surface.summary": "Capsule case, wireless earbuds, chest camera — three components covering visual capture, voice interaction, and tactile feedback.",
  "scenes.surface.stageLabel": "Product overview",
  "scenes.surface.stageSummary": "Three pieces, each with its role.",
  "scenes.surface.detail0.label": "Capsule case",
  "scenes.surface.detail0.body": "Touch panel + earbud charging dock + status LED, molded as one.",
  "scenes.surface.detail1.label": "Chest camera",
  "scenes.surface.detail1.body": "8g magnetic module. Silent standby, press to capture.",
  "scenes.surface.detail2.label": "Wireless earbuds",
  "scenes.surface.detail2.body": "AI results delivered by voice in real time. Hands and eyes free.",
  "scenes.surface.tags": "Capsule,Camera,Earbuds",
  "scenes.geometry.eyebrow": "Structure",
  "scenes.geometry.title": "Open the case, grab your gear.",
  "scenes.geometry.summary": "112° magnetic hinge opens one-handed. Earbuds pop up into place. Camera detaches magnetically for chest wear.",
  "scenes.geometry.stageLabel": "Structural detail",
  "scenes.geometry.stageSummary": "Every step has tactile confirmation.",
  "scenes.geometry.detail0.label": "Hinge",
  "scenes.geometry.detail0.body": "Precision-damped hinge with a definite feel and angle hold.",
  "scenes.geometry.detail1.label": "Earbud pop-up",
  "scenes.geometry.detail1.body": "Earbuds lift slightly when the lid opens for one-handed retrieval.",
  "scenes.geometry.detail2.label": "Camera detach",
  "scenes.geometry.detail2.body": "Magnetic latch releases — clip it to your chest and it enters standby.",
  "scenes.geometry.tags": "Hinge,Pop-up,Magnetic",
  "scenes.wear.eyebrow": "Daily Use",
  "scenes.wear.title": "Put it on. Start your day.",
  "scenes.wear.summary": "The camera reads your surroundings from your chest; the earbuds relay AI feedback. Commute, work, explore — it follows you.",
  "scenes.wear.stageLabel": "Daily wear",
  "scenes.wear.stageSummary": "Light enough to forget.",
  "scenes.wear.detail0.label": "Wear",
  "scenes.wear.detail0.body": "Camera 8g, earbuds 4.5g — barely noticeable.",
  "scenes.wear.detail1.label": "Trigger",
  "scenes.wear.detail1.body": "Press the case button or tap the earbud to start sensing.",
  "scenes.wear.detail2.label": "Response",
  "scenes.wear.detail2.body": "Results delivered through the earbuds within 2–5 seconds.",
  "scenes.wear.tags": "Wear,Trigger,Response",
  "scenes.engineering.eyebrow": "Engineering",
  "scenes.engineering.title": "Inspectable. Disassemblable. Production-ready.",
  "scenes.engineering.summary": "X-ray views, material callouts, tolerance checks — a full engineering perspective from design to manufacturing.",
  "scenes.engineering.stageLabel": "Engineering view",
  "scenes.engineering.stageSummary": "From render to mass production.",
  "scenes.engineering.detail0.label": "Exploded view",
  "scenes.engineering.detail0.body": "Every part layered out, assembly relationships at a glance.",
  "scenes.engineering.detail1.label": "Material callouts",
  "scenes.engineering.detail1.body": "Each component labeled with material, process, and surface finish.",
  "scenes.engineering.detail2.label": "Manufacturing-ready",
  "scenes.engineering.detail2.body": "Export engineering files directly to start tooling.",
  "scenes.engineering.tags": "Exploded,Material,Manufacturing"
}
```

- [ ] **Step 7: Create `messages/zh/ecosystem.json`**

```json
{
  "meta.title": "AI 生态 - 铭润科技",
  "hero.eyebrow": "AI Ecosystem",
  "hero.title": "不只是硬件。",
  "hero.body": "完整的 AI 工作台——从数据采集到模型训练，从个性化调优到云端部署，让设备越用越聪明。",
  "capabilities.workspace.title": "数据工作台",
  "capabilities.workspace.body": "上传、标注、版本管理——端到端管理你的训练数据。",
  "capabilities.training.title": "模型训练",
  "capabilities.training.body": "一键启动训练任务，实时查看损失曲线和日志。",
  "capabilities.deployment.title": "个性化部署",
  "capabilities.deployment.body": "将模型发布到你的设备，离线也能推理。",
  "capabilities.sync.title": "云端同步",
  "capabilities.sync.body": "模型、数据和设置跨设备同步，始终保持最新。",
  "workflow.eyebrow": "工作流",
  "workflow.title": "从体验到上线，四步完成。",
  "workflow.step0.label": "01",
  "workflow.step0.title": "体验",
  "workflow.step0.body": "打开 Demo，感受环境 AI 能力。",
  "workflow.step1.label": "02",
  "workflow.step1.title": "数据",
  "workflow.step1.body": "上传样本，创建数据集版本。",
  "workflow.step2.label": "03",
  "workflow.step2.title": "训练",
  "workflow.step2.body": "提交训练任务，查看评测结果。",
  "workflow.step3.label": "04",
  "workflow.step3.title": "发布",
  "workflow.step3.body": "注册模型，切换线上版本。"
}
```

- [ ] **Step 8: Create `messages/en/ecosystem.json`**

```json
{
  "meta.title": "AI Ecosystem - Mingrun Tech",
  "hero.eyebrow": "AI Ecosystem",
  "hero.title": "More than hardware.",
  "hero.body": "A complete AI workbench — from data collection to model training, from personalization to cloud deployment. Your device gets smarter over time.",
  "capabilities.workspace.title": "Data Workspace",
  "capabilities.workspace.body": "Upload, annotate, and version — manage your training data end-to-end.",
  "capabilities.training.title": "Model Training",
  "capabilities.training.body": "Launch training jobs with one click. Watch loss curves and logs in real time.",
  "capabilities.deployment.title": "Personalized Deployment",
  "capabilities.deployment.body": "Push models to your devices. Inference works offline.",
  "capabilities.sync.title": "Cloud Sync",
  "capabilities.sync.body": "Keep models, data, and settings synchronized across devices.",
  "workflow.eyebrow": "Workflow",
  "workflow.title": "From trial to production in four steps.",
  "workflow.step0.label": "01",
  "workflow.step0.title": "Try",
  "workflow.step0.body": "Open the demo and experience environment AI firsthand.",
  "workflow.step1.label": "02",
  "workflow.step1.title": "Data",
  "workflow.step1.body": "Upload samples and create dataset versions.",
  "workflow.step2.label": "03",
  "workflow.step2.title": "Train",
  "workflow.step2.body": "Submit training jobs and review evaluation results.",
  "workflow.step3.label": "04",
  "workflow.step3.title": "Deploy",
  "workflow.step3.body": "Register models and switch production versions."
}
```

- [ ] **Step 9: Create `messages/zh/demo.json`**

```json
{
  "meta.title": "在线体验 - 铭润科技",
  "hero.eyebrow": "Demo",
  "hero.title": "先试用，再决定。",
  "hero.body": "上传一张照片或使用摄像头，立即看到 AI 的环境理解能力。",
  "task.vqa": "视觉问答",
  "task.ocr": "文字识别",
  "status.offline": "离线",
  "status.online": "在线",
  "status.hybrid": "混合模式",
  "result.empty": "上传图片或拍照开始体验。",
  "result.loading": "正在分析...",
  "control.upload": "上传图片",
  "control.camera": "使用摄像头"
}
```

- [ ] **Step 10: Create `messages/en/demo.json`**

```json
{
  "meta.title": "Demo - Mingrun Tech",
  "hero.eyebrow": "Demo",
  "hero.title": "Try before you decide.",
  "hero.body": "Upload a photo or use your camera to see the AI's environment understanding in action.",
  "task.vqa": "Visual Q&A",
  "task.ocr": "Text Recognition",
  "status.offline": "Offline",
  "status.online": "Online",
  "status.hybrid": "Hybrid",
  "result.empty": "Upload an image or take a photo to get started.",
  "result.loading": "Analyzing...",
  "control.upload": "Upload image",
  "control.camera": "Use camera"
}
```

- [ ] **Step 11: Create `messages/zh/pricing.json`**

```json
{
  "meta.title": "定价 - 铭润科技",
  "hero.eyebrow": "Pricing",
  "hero.title": "选择适合你的方案。",
  "hero.body": "先用 Demo 感受产品能力，准备好了再进入完整工作流。按你的节奏来。",
  "tiers.explore.label": "Explore",
  "tiers.explore.title": "免费体验",
  "tiers.explore.body": "公开 Demo、基础推理、无需注册。",
  "tiers.explore.meta": "免费",
  "tiers.studio.label": "Studio",
  "tiers.studio.title": "完整工作流",
  "tiers.studio.body": "项目、数据集、训练、模型仓、发布。",
  "tiers.studio.meta": "候补中",
  "tiers.team.label": "Team",
  "tiers.team.title": "团队协作",
  "tiers.team.body": "多人 workspace、弹性配额、权限管理。",
  "tiers.team.meta": "即将推出"
}
```

- [ ] **Step 12: Create `messages/en/pricing.json`**

```json
{
  "meta.title": "Pricing - Mingrun Tech",
  "hero.eyebrow": "Pricing",
  "hero.title": "Pick the plan that fits.",
  "hero.body": "Start with the free demo to see what it can do, then move to the full workflow when you're ready. At your own pace.",
  "tiers.explore.label": "Explore",
  "tiers.explore.title": "Free trial",
  "tiers.explore.body": "Public demo, basic inference, no sign-up required.",
  "tiers.explore.meta": "Free",
  "tiers.studio.label": "Studio",
  "tiers.studio.title": "Full workflow",
  "tiers.studio.body": "Projects, datasets, training, model registry, deployment.",
  "tiers.studio.meta": "Waitlist",
  "tiers.team.label": "Team",
  "tiers.team.title": "Team collaboration",
  "tiers.team.body": "Multi-user workspace, elastic quotas, role-based access.",
  "tiers.team.meta": "Coming soon"
}
```

- [ ] **Step 13: Create `messages/zh/support.json`**

```json
{
  "meta.title": "支持 - 铭润科技",
  "hero.eyebrow": "Support",
  "hero.title": "文档、常见问题和联系方式。",
  "docs.title": "文档路径",
  "docs.start.label": "Start",
  "docs.start.title": "快速开始",
  "docs.start.body": "5 分钟跑通核心流程。",
  "docs.product.label": "Product",
  "docs.product.title": "产品指南",
  "docs.product.body": "硬件操作和模式切换。",
  "docs.workflow.label": "Workflow",
  "docs.workflow.title": "工作流",
  "docs.workflow.body": "数据、训练、发布。",
  "docs.security.label": "Security",
  "docs.security.title": "安全",
  "docs.security.body": "加密、访问控制、审计。",
  "faq.title": "常见问题",
  "faq.q0": "铭润科技 AI 耳机支持哪些设备？",
  "faq.a0": "目前支持铭润科技圆盘盒系列。更多设备适配即将推出。",
  "faq.q1": "数据安全如何保障？",
  "faq.a1": "所有数据加密传输和存储，支持离线模式下完全本地处理。",
  "faq.q2": "训练任务需要多久？",
  "faq.a2": "取决于数据集规模。典型任务 5-30 分钟，可在控制台实时查看进度。",
  "faq.q3": "如何开始使用？",
  "faq.a3": "访问 Demo 页面即可免费体验核心功能，无需注册。",
  "contact.title": "联系我们",
  "contact.email": "hello@mingrun-tech.com",
  "contact.response": "48 小时内回复。"
}
```

- [ ] **Step 14: Create `messages/en/support.json`**

```json
{
  "meta.title": "Support - Mingrun Tech",
  "hero.eyebrow": "Support",
  "hero.title": "Documentation, FAQ, and contact info.",
  "docs.title": "Documentation",
  "docs.start.label": "Start",
  "docs.start.title": "Quick Start",
  "docs.start.body": "Run through the core workflow in 5 minutes.",
  "docs.product.label": "Product",
  "docs.product.title": "Product Guide",
  "docs.product.body": "Hardware operation and mode switching.",
  "docs.workflow.label": "Workflow",
  "docs.workflow.title": "Workflow",
  "docs.workflow.body": "Data, training, deployment.",
  "docs.security.label": "Security",
  "docs.security.title": "Security",
  "docs.security.body": "Encryption, access control, audit.",
  "faq.title": "FAQ",
  "faq.q0": "What devices does the Mingrun Tech AI Earphone support?",
  "faq.a0": "Currently supports the Mingrun Tech capsule case series. More device support coming soon.",
  "faq.q1": "How is data security handled?",
  "faq.a1": "All data is encrypted in transit and at rest. Offline mode processes everything locally.",
  "faq.q2": "How long do training jobs take?",
  "faq.a2": "Depends on dataset size. Typical jobs run 5–30 minutes. Track progress in the console in real time.",
  "faq.q3": "How do I get started?",
  "faq.a3": "Visit the Demo page to try core features for free — no sign-up required.",
  "contact.title": "Contact Us",
  "contact.email": "hello@mingrun-tech.com",
  "contact.response": "We respond within 48 hours."
}
```

- [ ] **Step 15: Create `messages/zh/updates.json`**

```json
{
  "meta.title": "动态 - 铭润科技",
  "hero.eyebrow": "Updates",
  "hero.title": "产品动态与更新日志。",
  "timeline.0.label": "2026.03",
  "timeline.0.title": "产品页升级",
  "timeline.0.body": "全新视觉设计和交互体验。",
  "timeline.1.label": "2026.03",
  "timeline.1.title": "Demo 优化",
  "timeline.1.body": "产品查看器前置，推理体验优化。",
  "timeline.2.label": "2026.03",
  "timeline.2.title": "安全加固",
  "timeline.2.body": "CSRF、限流、签名访问全面上线。"
}
```

- [ ] **Step 16: Create `messages/en/updates.json`**

```json
{
  "meta.title": "Updates - Mingrun Tech",
  "hero.eyebrow": "Updates",
  "hero.title": "Product updates and changelog.",
  "timeline.0.label": "2026.03",
  "timeline.0.title": "Product page redesign",
  "timeline.0.body": "New visual design and interactive experience.",
  "timeline.1.label": "2026.03",
  "timeline.1.title": "Demo improvements",
  "timeline.1.body": "Product viewer front and center, better inference experience.",
  "timeline.2.label": "2026.03",
  "timeline.2.title": "Security hardening",
  "timeline.2.body": "CSRF protection, rate limiting, and signed URLs now live."
}
```

- [ ] **Step 17: Create `messages/zh/auth.json`**

```json
{
  "login.meta.title": "登录 - 铭润科技",
  "login.eyebrow": "铭润科技控制台",
  "login.heroTitle": "进入你的工作台，把数据、训练和模型发布收在一处。",
  "login.heroSummary": "公开站负责展示，控制台负责推进工作。登录后直接回到更克制、更高效的操作界面。",
  "login.point0.label": "私有",
  "login.point0.detail": "样本和产物读取走受管签名。",
  "login.point1.label": "可回滚",
  "login.point1.detail": "alias 发布和回退留在同一条线上。",
  "login.point2.label": "可追溯",
  "login.point2.detail": "关键动作会保留上下文。",
  "login.kicker": "登录",
  "login.title": "登录控制台",
  "login.helper": "继续进入你的 workspace。",
  "login.email.label": "邮箱",
  "login.email.placeholder": "邮箱",
  "login.password.label": "密码",
  "login.password.placeholder": "密码",
  "login.submit": "登录",
  "login.error": "登录失败",
  "login.noAccount": "还没有账号？",
  "login.register": "去注册",
  "login.studioAccess": "控制台",
  "register.meta.title": "注册 - 铭润科技",
  "register.eyebrow": "创建工作台",
  "register.heroTitle": "注册后直接进入工作台，不再在前台和后台之间切换语气。",
  "register.heroSummary": "默认会创建一个初始 workspace，把项目、数据集、训练和模型仓收进同一套操作面板。",
  "register.point0.label": "项目",
  "register.point0.detail": "按业务或实验边界组织工作。",
  "register.point1.label": "数据集",
  "register.point1.detail": "上传、标注、冻结版本。",
  "register.point2.label": "模型仓",
  "register.point2.detail": "发布 alias，并保留回滚路径。",
  "register.kicker": "注册",
  "register.title": "创建账号",
  "register.helper": "默认会创建初始 workspace。",
  "register.displayName.label": "显示名",
  "register.displayName.placeholder": "显示名",
  "register.email.label": "邮箱",
  "register.email.placeholder": "邮箱",
  "register.password.label": "密码",
  "register.password.placeholder": "密码",
  "register.submit": "注册并进入控制台",
  "register.error": "注册失败",
  "register.hasAccount": "已有账号？",
  "register.login": "去登录",
  "register.studioAccess": "控制台"
}
```

- [ ] **Step 18: Create `messages/en/auth.json`**

```json
{
  "login.meta.title": "Sign In - Mingrun Tech",
  "login.eyebrow": "Mingrun Tech Console",
  "login.heroTitle": "Your workbench for data, training, and model deployment — all in one place.",
  "login.heroSummary": "The public site is for discovery. The console is where real work happens. Sign in to pick up where you left off.",
  "login.point0.label": "Private",
  "login.point0.detail": "Samples and artifacts accessed via managed signed URLs.",
  "login.point1.label": "Rollback-ready",
  "login.point1.detail": "Alias-based deployment and rollback on the same track.",
  "login.point2.label": "Auditable",
  "login.point2.detail": "Key actions retain full context.",
  "login.kicker": "Sign In",
  "login.title": "Sign in to the console",
  "login.helper": "Continue to your workspace.",
  "login.email.label": "Email",
  "login.email.placeholder": "Email",
  "login.password.label": "Password",
  "login.password.placeholder": "Password",
  "login.submit": "Sign In",
  "login.error": "Sign-in failed",
  "login.noAccount": "Don't have an account?",
  "login.register": "Register",
  "login.studioAccess": "Console",
  "register.meta.title": "Register - Mingrun Tech",
  "register.eyebrow": "Create Workspace",
  "register.heroTitle": "Register and go straight to your workbench — no switching between public site and console.",
  "register.heroSummary": "A default workspace is created automatically, consolidating projects, datasets, training, and model registry in one panel.",
  "register.point0.label": "Projects",
  "register.point0.detail": "Organize work by business line or experiment.",
  "register.point1.label": "Datasets",
  "register.point1.detail": "Upload, annotate, freeze versions.",
  "register.point2.label": "Model registry",
  "register.point2.detail": "Publish aliases and keep rollback paths.",
  "register.kicker": "Register",
  "register.title": "Create account",
  "register.helper": "A default workspace will be created.",
  "register.displayName.label": "Display name",
  "register.displayName.placeholder": "Display name",
  "register.email.label": "Email",
  "register.email.placeholder": "Email",
  "register.password.label": "Password",
  "register.password.placeholder": "Password",
  "register.submit": "Register and enter console",
  "register.error": "Registration failed",
  "register.hasAccount": "Already have an account?",
  "register.login": "Sign in",
  "register.studioAccess": "Console"
}
```

- [ ] **Step 19: Create `messages/zh/error.json`**

```json
{
  "notFound.kicker": "404",
  "notFound.title": "这个页面不存在，或者你当前不该看到它。",
  "notFound.body": "如果你是从控制台深链进入，可能是资源已删除或当前 workspace 无权限；如果你在公开站里跳转，这通常只是一个无效链接。",
  "notFound.home": "返回首页",
  "notFound.demo": "打开 Demo"
}
```

- [ ] **Step 20: Create `messages/en/error.json`**

```json
{
  "notFound.kicker": "404",
  "notFound.title": "This page doesn't exist, or you shouldn't be seeing it right now.",
  "notFound.body": "If you followed a deep link from the console, the resource may have been deleted or your workspace may not have access. If you navigated from the public site, this is likely just a broken link.",
  "notFound.home": "Back to home",
  "notFound.demo": "Open Demo"
}
```

- [ ] **Step 21: Create `messages/zh/console.json`**

```json
{
  "brand": "铭润科技控制台",
  "nav.dashboard": "仪表盘",
  "nav.projects": "项目",
  "nav.datasets": "数据集",
  "nav.train": "训练",
  "nav.models": "模型仓",
  "nav.eval": "评测",
  "nav.settings": "设置",
  "nav.devices": "设备",
  "nav.billing": "计费",
  "nav.comingSoon": "(即将推出)",
  "nav.backToSite": "返回网站",
  "breadcrumb.app": "仪表盘",
  "breadcrumb.projects": "项目",
  "breadcrumb.datasets": "数据集",
  "breadcrumb.train": "训练",
  "breadcrumb.models": "模型仓",
  "breadcrumb.eval": "评测",
  "breadcrumb.settings": "设置",
  "breadcrumb.devices": "设备",
  "breadcrumb.billing": "计费",
  "topbar.selectProject": "选择项目",
  "topbar.settings": "设置",
  "topbar.backToSite": "返回网站",
  "topbar.userMenu": "用户菜单",
  "topbar.openMenu": "打开菜单",
  "statusbar.noProject": "无项目",
  "statusbar.apiConnected": "API 已连接",
  "cmd.placeholder": "输入命令或搜索...",
  "cmd.empty": "没有找到结果。",
  "cmd.navigate": "导航",
  "cmd.actions": "操作",
  "cmd.newProject": "新建项目",
  "cmd.newDataset": "新建数据集",
  "cmd.startTraining": "启动训练任务",
  "mobile.dashboard.meta": "总览",
  "mobile.projects.meta": "项目",
  "mobile.datasets.meta": "数据集",
  "mobile.train.meta": "训练",
  "mobile.models.meta": "模型仓",
  "mobile.eval.meta": "评测",
  "mobile.settings.meta": "设置"
}
```

- [ ] **Step 22: Create `messages/en/console.json`**

```json
{
  "brand": "Mingrun Tech Console",
  "nav.dashboard": "Dashboard",
  "nav.projects": "Projects",
  "nav.datasets": "Datasets",
  "nav.train": "Train",
  "nav.models": "Models",
  "nav.eval": "Eval",
  "nav.settings": "Settings",
  "nav.devices": "Devices",
  "nav.billing": "Billing",
  "nav.comingSoon": "(coming soon)",
  "nav.backToSite": "Back to site",
  "breadcrumb.app": "Dashboard",
  "breadcrumb.projects": "Projects",
  "breadcrumb.datasets": "Datasets",
  "breadcrumb.train": "Train",
  "breadcrumb.models": "Models",
  "breadcrumb.eval": "Eval",
  "breadcrumb.settings": "Settings",
  "breadcrumb.devices": "Devices",
  "breadcrumb.billing": "Billing",
  "topbar.selectProject": "Select project",
  "topbar.settings": "Settings",
  "topbar.backToSite": "Back to site",
  "topbar.userMenu": "User menu",
  "topbar.openMenu": "Open menu",
  "statusbar.noProject": "No project",
  "statusbar.apiConnected": "API Connected",
  "cmd.placeholder": "Type a command or search...",
  "cmd.empty": "No results found.",
  "cmd.navigate": "Navigate",
  "cmd.actions": "Actions",
  "cmd.newProject": "Create new project",
  "cmd.newDataset": "Create new dataset",
  "cmd.startTraining": "Start training job",
  "mobile.dashboard.meta": "Overview",
  "mobile.projects.meta": "Projects",
  "mobile.datasets.meta": "Datasets",
  "mobile.train.meta": "Train",
  "mobile.models.meta": "Models",
  "mobile.eval.meta": "Eval",
  "mobile.settings.meta": "Settings"
}
```

- [ ] **Step 23: Create console page translation stubs (zh)**

Create each file with page-specific strings. These are smaller files with section headers, descriptions, and empty-state text.

`messages/zh/console-projects.json`:
```json
{
  "kicker": "Projects",
  "title": "项目",
  "description": "按业务或实验边界组织工作。",
  "detail.kicker": "Projects",
  "detail.title": "项目详情",
  "detail.description": "查看项目内容。",
  "loading": "加载中..."
}
```

`messages/zh/console-datasets.json`:
```json
{
  "kicker": "Datasets",
  "title": "数据集",
  "description": "上传、标注、管理你的训练数据。"
}
```

`messages/zh/console-train.json`:
```json
{
  "kicker": "Train",
  "title": "训练",
  "description": "提交训练任务，查看评测结果。"
}
```

`messages/zh/console-models.json`:
```json
{
  "kicker": "Models",
  "title": "模型仓",
  "description": "管理模型版本和发布。"
}
```

`messages/zh/console-eval.json`:
```json
{
  "kicker": "Eval",
  "title": "评测",
  "description": "对比模型性能和指标。"
}
```

`messages/zh/console-settings.json`:
```json
{
  "kicker": "Settings",
  "title": "账户与安全",
  "description": "管理账户、安全和后续敏感操作入口。",
  "panel.title": "账户操作",
  "panel.description": "当前版本先保留登录管理和删除流程入口，后续会补上 API Key 与团队设置。"
}
```

`messages/zh/console-devices.json`:
```json
{
  "kicker": "Devices",
  "title": "设备与固件",
  "description": "为设备绑定、固件版本和硬件诊断预留入口。",
  "panel.title": "设备工作区",
  "panel.description": "设备绑定、固件升级和硬件诊断还没接入，但信息架构已经按后续方向预留。",
  "empty": "当前还没有已绑定设备。后续将展示序列号、固件版本、电量和隐私开关状态。"
}
```

`messages/zh/console-billing.json`:
```json
{
  "kicker": "Billing",
  "title": "计费与套餐",
  "description": "为后续算力、存储和团队额度管理保留信息架构。",
  "panel.title": "计费与额度",
  "panel.description": "v0.1 还没有正式计费，但后续会在这里管理算力、存储和团队额度。",
  "empty": "当前版本不产生真实账单。套餐结构和配额规则会在控制台正式开放前补齐。"
}
```

- [ ] **Step 24: Create console page translation stubs (en)**

`messages/en/console-projects.json`:
```json
{
  "kicker": "Projects",
  "title": "Projects",
  "description": "Organize work by business line or experiment.",
  "detail.kicker": "Projects",
  "detail.title": "Project details",
  "detail.description": "View project contents.",
  "loading": "Loading..."
}
```

`messages/en/console-datasets.json`:
```json
{
  "kicker": "Datasets",
  "title": "Datasets",
  "description": "Upload, annotate, and manage your training data."
}
```

`messages/en/console-train.json`:
```json
{
  "kicker": "Train",
  "title": "Training",
  "description": "Submit training jobs and review evaluation results."
}
```

`messages/en/console-models.json`:
```json
{
  "kicker": "Models",
  "title": "Models",
  "description": "Manage model versions and deployments."
}
```

`messages/en/console-eval.json`:
```json
{
  "kicker": "Eval",
  "title": "Evaluation",
  "description": "Compare model performance and metrics."
}
```

`messages/en/console-settings.json`:
```json
{
  "kicker": "Settings",
  "title": "Account & Security",
  "description": "Manage your account, security, and sensitive operations.",
  "panel.title": "Account actions",
  "panel.description": "Login management and account deletion for now. API keys and team settings coming later."
}
```

`messages/en/console-devices.json`:
```json
{
  "kicker": "Devices",
  "title": "Devices & Firmware",
  "description": "Device pairing, firmware versions, and hardware diagnostics.",
  "panel.title": "Device workspace",
  "panel.description": "Device pairing, firmware updates, and hardware diagnostics are not yet connected, but the layout is ready for what's next.",
  "empty": "No paired devices yet. Serial numbers, firmware versions, battery levels, and privacy toggles will appear here."
}
```

`messages/en/console-billing.json`:
```json
{
  "kicker": "Billing",
  "title": "Billing & Plans",
  "description": "Compute, storage, and team quota management.",
  "panel.title": "Billing & Quotas",
  "panel.description": "No billing in v0.1, but this is where you'll manage compute, storage, and team quotas.",
  "empty": "No real charges in the current version. Plan structures and quota rules will be finalized before the console opens publicly."
}
```

- [ ] **Step 25: Commit all translation files**

```bash
git add apps/web/messages/
git commit -m "feat(i18n): add all translation files for zh and en"
```

---

### Task 4: Restructure layouts and move route groups

**Files:**
- Modify: `apps/web/app/layout.tsx`
- Create: `apps/web/app/[locale]/layout.tsx`
- Move: `apps/web/app/(public)/` → `apps/web/app/[locale]/(public)/`
- Move: `apps/web/app/(console)/` → `apps/web/app/[locale]/(console)/`
- Move: `apps/web/app/login/` → `apps/web/app/[locale]/(auth)/login/`
- Move: `apps/web/app/register/` → `apps/web/app/[locale]/(auth)/register/`
- Move: `apps/web/app/not-found.tsx` → `apps/web/app/[locale]/not-found.tsx`

**Important:** After this task, the app will NOT build until page migrations in Chunk 2 and 3 are complete. This is expected — the route restructure and page content migration must be treated as one atomic unit. Do not attempt `npx tsc` or `next build` until Task 13.

- [ ] **Step 1: Simplify root layout to pass-through**

Replace `apps/web/app/layout.tsx` with:

```tsx
export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  // next-intl requires the [locale] layout to own <html>/<body>.
  // Root layout is a pass-through.
  return children as React.ReactElement;
}
```

- [ ] **Step 2: Create `apps/web/app/[locale]/layout.tsx`**

```tsx
import type { Metadata } from "next";
import { Inter, JetBrains_Mono } from "next/font/google";
import { NextIntlClientProvider } from "next-intl";
import { getMessages, getTranslations } from "next-intl/server";
import { notFound } from "next/navigation";

import { Providers } from "@/components/providers";
import { routing } from "@/i18n/routing";
import "@/styles/globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-sans",
  display: "swap",
});

const jetbrainsMono = JetBrains_Mono({
  subsets: ["latin"],
  variable: "--font-mono",
  display: "swap",
});

export function generateStaticParams() {
  return routing.locales.map((locale) => ({ locale }));
}

export async function generateMetadata(): Promise<Metadata> {
  const t = await getTranslations("common");
  const th = await getTranslations("home");
  return {
    title: {
      template: `%s - ${t("brand.company")}`,
      default: th("meta.title"),
    },
    description: th("meta.description"),
  };
}

export default async function LocaleLayout({
  children,
  params,
}: {
  children: React.ReactNode;
  params: Promise<{ locale: string }>;
}) {
  const { locale } = await params;

  if (!routing.locales.includes(locale as any)) {
    notFound();
  }

  const messages = await getMessages();

  return (
    <html lang={locale} suppressHydrationWarning>
      <body className={`${inter.variable} ${jetbrainsMono.variable}`}>
        <NextIntlClientProvider messages={messages}>
          <Providers>{children}</Providers>
        </NextIntlClientProvider>
      </body>
    </html>
  );
}
```

- [ ] **Step 3: Move route groups under [locale]**

```bash
cd /Users/dog/Desktop/铭润/apps/web/app
mkdir -p "[locale]"
git mv "(public)" "[locale]/(public)"
git mv "(console)" "[locale]/(console)"
mkdir -p "[locale]/(auth)"
git mv login "[locale]/(auth)/login"
git mv register "[locale]/(auth)/register"
git mv not-found.tsx "[locale]/not-found.tsx"
```

- [ ] **Step 4: Verify directory structure**

```bash
ls -la /Users/dog/Desktop/铭润/apps/web/app/\[locale\]/
```

Expected: `(public)/`, `(console)/`, `(auth)/`, `layout.tsx`, `not-found.tsx`

- [ ] **Step 5: Commit**

```bash
git add apps/web/app/
git commit -m "feat(i18n): restructure routes under [locale] segment"
```

---

### Task 5: Migrate shared components — SiteHeader, SiteFooter, MobileNav, PublicDocumentLink

**Files:**
- Modify: `apps/web/components/public/SiteHeader.tsx`
- Modify: `apps/web/components/public/SiteFooter.tsx`
- Modify: `apps/web/components/public/MobileNav.tsx`
- Modify: `apps/web/components/PublicDocumentLink.tsx`
- Create: `apps/web/components/LanguageSwitcher.tsx`

- [ ] **Step 1: Replace PublicDocumentLink's `next/link` with locale-aware Link**

In `apps/web/components/PublicDocumentLink.tsx`, change the import:

```tsx
// Before
import Link from "next/link";

// After
import { Link } from "@/i18n/navigation";
```

No other changes needed — the component interface stays the same.

- [ ] **Step 2: Create `apps/web/components/LanguageSwitcher.tsx`**

```tsx
"use client";

import { useLocale } from "next-intl";
import { usePathname, useRouter } from "@/i18n/navigation";

export function LanguageSwitcher({ className }: { className?: string }) {
  const locale = useLocale();
  const router = useRouter();
  const pathname = usePathname();

  const switchLocale = () => {
    const next = locale === "zh" ? "en" : "zh";
    router.replace(pathname, { locale: next });
  };

  return (
    <button
      onClick={switchLocale}
      className={className}
      aria-label={locale === "zh" ? "Switch to English" : "切换到中文"}
    >
      {locale === "zh" ? "EN" : "中"}
    </button>
  );
}
```

- [ ] **Step 3: Rewrite SiteHeader with translations**

Replace the full content of `apps/web/components/public/SiteHeader.tsx`:

```tsx
"use client";

import { useState, useCallback } from "react";
import clsx from "clsx";
import { useTranslations } from "next-intl";
import { usePathname } from "@/i18n/navigation";

import { PublicDocumentLink } from "@/components/PublicDocumentLink";
import { MobileNav } from "@/components/public/MobileNav";
import { LanguageSwitcher } from "@/components/LanguageSwitcher";
import { useScrollNav } from "@/lib/useScrollNav";

const NAV_KEYS = [
  { href: "/product", key: "nav.product" },
  { href: "/ecosystem", key: "nav.ecosystem" },
  { href: "/demo", key: "nav.demo" },
  { href: "/pricing", key: "nav.pricing" },
  { href: "/support", key: "nav.support" },
] as const;

export function SiteHeader() {
  const t = useTranslations("common");
  const pathname = usePathname();
  const { scrolled, hidden, progress } = useScrollNav();
  const [mobileOpen, setMobileOpen] = useState(false);

  const closeMobile = useCallback(() => setMobileOpen(false), []);

  const navItems = NAV_KEYS.map((item) => ({
    href: item.href,
    label: t(item.key),
  }));

  return (
    <>
      <header
        className={clsx(
          "site-header-v2",
          scrolled && "is-scrolled",
          hidden && "is-hidden",
        )}
      >
        <div className="mx-auto flex h-full max-w-[var(--site-width)] items-center justify-between px-6">
          <PublicDocumentLink href="/" className="flex items-center gap-2">
            <span className="h-2 w-2 rounded-full bg-[var(--brand-v2)]" />
            <strong className="text-base font-semibold tracking-tight">
              {t("brand.company")}
            </strong>
          </PublicDocumentLink>

          <nav className="hidden items-center gap-8 md:flex">
            {navItems.map((item) => (
              <PublicDocumentLink
                key={item.href}
                href={item.href}
                className={clsx(
                  "site-nav-link-v2 text-sm transition-colors",
                  pathname === item.href
                    ? "text-[var(--brand-v2)]"
                    : "text-[var(--text-secondary)] hover:text-[var(--text-primary)]",
                )}
              >
                {item.label}
                {pathname === item.href && (
                  <span className="absolute bottom-0 left-0 h-0.5 w-full bg-[var(--brand-v2)]" />
                )}
              </PublicDocumentLink>
            ))}
            <LanguageSwitcher className="text-sm text-[var(--text-secondary)] hover:text-[var(--text-primary)] transition-colors" />
            <PublicDocumentLink
              href="/demo"
              className="rounded-[var(--radius-full)] bg-[var(--brand-v2)] px-5 py-2 text-sm font-medium text-white transition-transform hover:scale-[1.02] active:scale-[0.98]"
            >
              {t("nav.getStarted")}
            </PublicDocumentLink>
          </nav>

          <button
            className="flex items-center justify-center p-2.5 md:hidden"
            onClick={() => setMobileOpen(true)}
            aria-label={t("nav.openMenu")}
          >
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">
              <path d="M4 6h16M4 12h16M4 18h16" />
            </svg>
          </button>
        </div>

        <div
          className="absolute bottom-0 left-0 h-[2px] bg-[var(--brand-v2)] transition-none"
          style={{ width: `${progress * 100}%` }}
          aria-hidden="true"
        />
      </header>

      <MobileNav
        open={mobileOpen}
        onClose={closeMobile}
        items={navItems}
        pathname={pathname}
      />
    </>
  );
}
```

- [ ] **Step 4: Rewrite SiteFooter with translations**

Replace the full content of `apps/web/components/public/SiteFooter.tsx`:

```tsx
"use client";

import { useState } from "react";
import { useTranslations } from "next-intl";
import { PublicDocumentLink } from "@/components/PublicDocumentLink";

export function SiteFooter() {
  const t = useTranslations("common");
  const [openSection, setOpenSection] = useState<string | null>(null);

  const toggle = (title: string) =>
    setOpenSection((prev) => (prev === title ? null : title));

  const columns = [
    {
      titleKey: "footer.product",
      links: [
        { href: "/product", labelKey: "footer.product.overview" },
        { href: "/product#specs", labelKey: "footer.product.specs" },
        { href: "/product#craftsmanship", labelKey: "footer.product.craftsmanship" },
      ],
    },
    {
      titleKey: "footer.ecosystem",
      links: [
        { href: "/ecosystem", labelKey: "footer.ecosystem.platform" },
        { href: "/demo", labelKey: "footer.ecosystem.demo" },
        { href: "/pricing", labelKey: "footer.ecosystem.pricing" },
      ],
    },
    {
      titleKey: "footer.support",
      links: [
        { href: "/support", labelKey: "footer.support.docs" },
        { href: "/support#faq", labelKey: "footer.support.faq" },
        { href: "/support#contact", labelKey: "footer.support.contact" },
      ],
    },
    {
      titleKey: "footer.about",
      links: [
        { href: "/updates", labelKey: "footer.about.updates" },
        { href: "mailto:hello@mingrun-tech.com", labelKey: "footer.about.email" },
      ],
    },
  ];

  return (
    <footer className="border-t border-[var(--border)] bg-[var(--bg-surface)] py-12">
      <div className="mx-auto grid max-w-[var(--site-width)] gap-8 px-6 md:grid-cols-4">
        {columns.map((col) => {
          const title = t(col.titleKey);
          return (
            <div key={col.titleKey}>
              <button
                className="flex w-full items-center justify-between text-sm font-semibold text-[var(--text-primary)] md:cursor-default md:pointer-events-none"
                onClick={() => toggle(col.titleKey)}
                aria-expanded={openSection === col.titleKey}
              >
                {title}
                <svg
                  className={`h-4 w-4 transition-transform md:hidden ${
                    openSection === col.titleKey ? "rotate-180" : ""
                  }`}
                  fill="none"
                  viewBox="0 0 24 24"
                  stroke="currentColor"
                  strokeWidth="2"
                >
                  <path d="M19 9l-7 7-7-7" />
                </svg>
              </button>
              <ul
                className={`mt-3 flex flex-col gap-2 overflow-hidden transition-[max-height] duration-300 md:max-h-none ${
                  openSection === col.titleKey ? "max-h-40" : "max-h-0 md:max-h-none"
                }`}
              >
                {col.links.map((link) => (
                  <li key={link.href}>
                    <PublicDocumentLink
                      href={link.href}
                      className="text-sm text-[var(--text-secondary)] hover:text-[var(--text-primary)] transition-colors"
                    >
                      {t(link.labelKey)}
                    </PublicDocumentLink>
                  </li>
                ))}
              </ul>
            </div>
          );
        })}
      </div>

      <div className="mx-auto mt-12 flex max-w-[var(--site-width)] flex-col items-center justify-between gap-4 border-t border-[var(--border)] px-6 pt-8 md:flex-row">
        <div className="flex items-center gap-2">
          <span className="h-2 w-2 rounded-full bg-[var(--brand-v2)]" />
          <strong className="text-sm font-semibold">{t("brand.company")}</strong>
        </div>
        <p className="text-xs text-[var(--text-secondary)]">
          {t("footer.copyright")}
        </p>
      </div>
    </footer>
  );
}
```

- [ ] **Step 5: Update MobileNav with translations**

In `apps/web/components/public/MobileNav.tsx`, change:

```tsx
// Replace "Try Demo" with translated text
// The component already receives items as props, so nav labels are already translated.
// Only need to change: aria-label and CTA button text.

// Import
import { useTranslations } from "next-intl";

// Inside component, add:
const t = useTranslations("common");

// Change aria-label="Close menu" to:
aria-label={t("nav.closeMenu")}

// Change "Try Demo" to:
{t("nav.tryDemo")}
```

- [ ] **Step 6: Commit**

```bash
git add apps/web/components/
git commit -m "feat(i18n): migrate SiteHeader, SiteFooter, MobileNav, add LanguageSwitcher"
```

---

## Chunk 2: Public Site Pages

### Task 6: Create home page data file and migrate home page

**Files:**
- Create: `apps/web/lib/home-data.ts`
- Modify: `apps/web/app/[locale]/(public)/page.tsx`
- Modify: `apps/web/components/public/HeroScene.tsx`
- Modify: `apps/web/components/public/EcosystemPreview.tsx`
- Delete: `apps/web/lib/home-content.ts` (after migration)

- [ ] **Step 1: Create `apps/web/lib/home-data.ts`**

Extract non-translatable data (viewerPatch, tone) from home-content.ts:

```ts
import type { ViewerState } from "@/lib/qihang-viewer-contract";

export interface HomeSceneData {
  id: string;
  viewerPatch?: Partial<ViewerState>;
  tone: "pearl" | "midnight" | "glacier";
}

export const HOME_SCENES: HomeSceneData[] = [
  {
    id: "hero",
    tone: "pearl",
    viewerPatch: { isOpen: false, colorway: "pearl", mode: "offline" },
  },
  {
    id: "highlights",
    tone: "pearl",
    viewerPatch: { isOpen: true, earbudsOut: true, colorway: "pearl" },
  },
  {
    id: "ecosystem",
    tone: "midnight",
    viewerPatch: { isOpen: true, mode: "online", colorway: "pearl", nightOn: false },
  },
  {
    id: "craft",
    tone: "glacier",
    viewerPatch: { isOpen: true, pivotAngleDeg: 88, pivotState: "opening", colorway: "glacier" },
  },
  {
    id: "cta",
    tone: "pearl",
    viewerPatch: { isOpen: false, colorway: "pearl" },
  },
];
```

- [ ] **Step 2: Migrate home page to use translations**

The home page (`apps/web/app/[locale]/(public)/page.tsx`) should:
- Import `getTranslations` from `next-intl/server`
- Import `HOME_SCENES` from `@/lib/home-data`
- Replace direct content references with `t()` calls
- Pass translated strings to child components

Key pattern:

```tsx
import { getTranslations } from "next-intl/server";
import { HOME_SCENES } from "@/lib/home-data";

export async function generateMetadata() {
  const t = await getTranslations("home");
  return { title: t("meta.title"), description: t("meta.description") };
}

export default async function HomePage() {
  const t = await getTranslations("home");
  const heroScene = HOME_SCENES[0];
  // Pass translated text + scene data to components
  // ...
}
```

- [ ] **Step 3: Update HeroScene to accept translated strings as props**

`apps/web/components/public/HeroScene.tsx` currently imports `HomeScene` type from `home-content`. Change it to accept explicit props:

```tsx
interface HeroSceneProps {
  eyebrow: string;
  title: string;
  body: string;
  imageAlt: string;
}

export function HeroScene({ eyebrow, title, body, imageAlt }: HeroSceneProps) {
  // Same rendering logic, but use props instead of scene.eyebrow, etc.
  // Change ImagePlaceholder label from hardcoded "Earphone Hero Shot / 3D Viewer" to imageAlt prop
}
```

- [ ] **Step 4: Update EcosystemPreview to accept translated strings**

Similarly update `apps/web/components/public/EcosystemPreview.tsx`:
- Accept translated text as props instead of `HomeScene`
- Change hardcoded "Learn More" to a prop
- Change hardcoded "AI Ecosystem Illustration" to a prop

- [ ] **Step 5: Update WorkbenchHero "Studio Access" text**

In `apps/web/components/WorkbenchHero.tsx`, the "Studio Access" string should be passed as a prop `statusLabel` instead of hardcoded. The auth pages will pass the translated value.

- [ ] **Step 6: Delete `apps/web/lib/home-content.ts`**

```bash
git rm apps/web/lib/home-content.ts
```

- [ ] **Step 7: Commit**

```bash
git add apps/web/lib/home-data.ts apps/web/app/\[locale\]/\(public\)/page.tsx apps/web/components/public/HeroScene.tsx apps/web/components/public/EcosystemPreview.tsx apps/web/components/WorkbenchHero.tsx
git commit -m "feat(i18n): migrate home page to translations, extract home-data.ts"
```

---

### Task 7: Create product page data file and migrate product page

**Files:**
- Create: `apps/web/lib/product-data.ts`
- Modify: `apps/web/app/[locale]/(public)/product/page.tsx`

- [ ] **Step 1: Create `apps/web/lib/product-data.ts`**

Extract non-translatable data from `public-story-content.ts` PRODUCT_STORY_SCENES:

```ts
import type { ViewerState } from "@/lib/qihang-viewer-contract";

export interface ProductSceneData {
  id: string;
  viewerPatch: Partial<ViewerState>;
  assetSlots: string[];
  tone: string;
}

export const PRODUCT_SCENES: ProductSceneData[] = [
  {
    id: "surface",
    tone: "pearl",
    assetSlots: ["产品主镜头", "材质近景", "状态切换短片"],
    viewerPatch: {
      isOpen: false, camDetached: false, earbudsOut: false,
      mode: "offline", colorway: "pearl", caseMode: "office_mode",
      xrayOn: false, nightOn: false, pivotInspectActive: false,
    },
  },
  {
    id: "geometry",
    tone: "midnight",
    assetSlots: ["转轴微距", "开盖动作", "结构示意"],
    viewerPatch: {
      isOpen: true, pivotAngleDeg: 88, pivotState: "opening",
      earbudsOut: true, camDetached: false, mode: "offline",
      colorway: "pearl", caseMode: "commute_mode",
    },
  },
  {
    id: "wear",
    tone: "glacier",
    assetSlots: ["佩戴场景", "触发动作", "结果联动画面"],
    viewerPatch: {
      isOpen: true, camDetached: true, earbudsOut: true,
      mode: "hybrid", colorway: "glacier", caseMode: "commute_mode",
      dreamOn: true,
    },
  },
  {
    id: "engineering",
    tone: "obsidian",
    assetSlots: ["爆炸视图", "材质说明", "制造状态录屏"],
    viewerPatch: {
      isOpen: true, camDetached: true, earbudsOut: true,
      mode: "online", colorway: "graphite", caseMode: "office_mode",
      xrayOn: true, pivotInspectActive: true, pivotExplodeActive: true,
      nightOn: true,
    },
  },
];
```

- [ ] **Step 2: Migrate product page to translations**

In `apps/web/app/[locale]/(public)/product/page.tsx`:
- Import `getTranslations` from `next-intl/server`
- Import `PRODUCT_SCENES` from `@/lib/product-data`
- Build scenes by merging data + translated strings:

```tsx
const t = await getTranslations("product");
const scenes = PRODUCT_SCENES.map((scene) => ({
  ...scene,
  eyebrow: t(`scenes.${scene.id}.eyebrow`),
  title: t(`scenes.${scene.id}.title`),
  summary: t(`scenes.${scene.id}.summary`),
  stageLabel: t(`scenes.${scene.id}.stageLabel`),
  stageSummary: t(`scenes.${scene.id}.stageSummary`),
  stageTags: t(`scenes.${scene.id}.tags`).split(","),
  details: [0, 1, 2].map((i) => ({
    label: t(`scenes.${scene.id}.detail${i}.label`),
    body: t(`scenes.${scene.id}.detail${i}.body`),
  })),
}));
```

- [ ] **Step 3: Commit**

```bash
git add apps/web/lib/product-data.ts apps/web/app/\[locale\]/\(public\)/product/
git commit -m "feat(i18n): migrate product page to translations"
```

---

### Task 8: Migrate ecosystem, pricing, support, updates, demo pages

**Files:**
- Create: `apps/web/lib/story-scene-data.ts` (non-translatable data for all story pages)
- Modify: `apps/web/app/[locale]/(public)/ecosystem/page.tsx`
- Modify: `apps/web/app/[locale]/(public)/pricing/page.tsx`
- Modify: `apps/web/app/[locale]/(public)/support/page.tsx`
- Modify: `apps/web/app/[locale]/(public)/updates/page.tsx`
- Modify: `apps/web/app/[locale]/(public)/demo/page.tsx`
- Delete: `apps/web/lib/public-story-content.ts` (after all pages migrated)

- [ ] **Step 1: Create `apps/web/lib/story-scene-data.ts`**

Extract non-translatable data (viewerPatch, tone, assetSlots) from `public-story-content.ts` for all non-product story pages. Use same pattern as `product-data.ts`:

```ts
export interface StorySceneData {
  id: string;
  viewerPatch?: Record<string, any>;
  tone: string;
  assetSlots?: string[];
}

// Workflow scenes (used on ecosystem page)
export const WORKFLOW_SCENES: StorySceneData[] = [
  { id: "demo", tone: "pearl" },
  { id: "dataset", tone: "midnight" },
  { id: "train", tone: "glacier" },
  { id: "release", tone: "obsidian" },
];

// Pricing scenes
export const PRICING_SCENES: StorySceneData[] = [
  { id: "entry", tone: "pearl" },
  { id: "explore", tone: "midnight" },
  { id: "studio", tone: "glacier" },
  { id: "team", tone: "obsidian" },
];

// Docs scenes (used on support page)
export const DOCS_SCENES: StorySceneData[] = [
  { id: "quickstart", tone: "pearl" },
  { id: "product-docs", tone: "midnight" },
  { id: "workflow-docs", tone: "glacier" },
  { id: "security-docs", tone: "obsidian" },
];

// Updates scenes
export const UPDATES_SCENES: StorySceneData[] = [
  { id: "march-product", tone: "pearl" },
  { id: "march-demo", tone: "midnight" },
  { id: "march-security", tone: "obsidian" },
];

// Contact scenes (if support page uses them)
export const CONTACT_SCENES: StorySceneData[] = [
  { id: "mail", tone: "pearl" },
  { id: "topics", tone: "midnight" },
  { id: "security", tone: "obsidian" },
];
```

- [ ] **Step 2: Migrate ecosystem page**

In `apps/web/app/[locale]/(public)/ecosystem/page.tsx`:

```tsx
import { getTranslations } from "next-intl/server";

export async function generateMetadata() {
  const t = await getTranslations("ecosystem");
  return { title: t("meta.title") };
}

export default async function EcosystemPage() {
  const t = await getTranslations("ecosystem");

  // Build capabilities from translation keys
  const capabilities = ["workspace", "training", "deployment", "sync"].map((key) => ({
    title: t(`capabilities.${key}.title`),
    body: t(`capabilities.${key}.body`),
  }));

  // Build workflow steps from translation keys
  const workflowSteps = [0, 1, 2, 3].map((i) => ({
    label: t(`workflow.step${i}.label`),
    title: t(`workflow.step${i}.title`),
    body: t(`workflow.step${i}.body`),
  }));

  // Replace hardcoded content with capabilities and workflowSteps arrays
  // ...render using same layout, passing translated data
}
```

- [ ] **Step 3: Migrate pricing page**

Same pattern — build tier items from `pricing` namespace:

```tsx
const tiers = ["explore", "studio", "team"].map((key) => ({
  label: t(`tiers.${key}.label`),
  title: t(`tiers.${key}.title`),
  body: t(`tiers.${key}.body`),
  meta: t(`tiers.${key}.meta`),
}));
```

- [ ] **Step 4: Migrate support page**

Build docs paths and FAQ items from `support` namespace:

```tsx
const docsPaths = ["start", "product", "workflow", "security"].map((key) => ({
  label: t(`docs.${key}.label`),
  title: t(`docs.${key}.title`),
  body: t(`docs.${key}.body`),
}));

const faqItems = [0, 1, 2, 3].map((i) => ({
  question: t(`faq.q${i}`),
  answer: t(`faq.a${i}`),
}));
```

- [ ] **Step 5: Migrate updates page**

```tsx
const timeline = [0, 1, 2].map((i) => ({
  label: t(`timeline.${i}.label`),
  title: t(`timeline.${i}.title`),
  body: t(`timeline.${i}.body`),
}));
```

- [ ] **Step 6: Migrate demo page**

Replace inline Chinese/English labels with `t()` calls from `demo` namespace.

- [ ] **Step 7: Delete old content files**

```bash
git rm apps/web/lib/public-story-content.ts
```

Verify no other imports remain:
```bash
grep -r "public-story-content" apps/web/ --include="*.ts" --include="*.tsx"
```

- [ ] **Step 8: Commit**

```bash
git add apps/web/lib/story-scene-data.ts apps/web/app/\[locale\]/\(public\)/ apps/web/lib/
git commit -m "feat(i18n): migrate all public pages to translations, remove old content files"
```

---

## Chunk 3: Console + Auth + Cleanup

### Task 9: Migrate console shared components

**Files:**
- Modify: `apps/web/components/console/ActivityBar.tsx`
- Modify: `apps/web/components/console/TopBar.tsx`
- Modify: `apps/web/components/console/StatusBar.tsx`
- Modify: `apps/web/components/console/CommandPalette.tsx`
- Modify: `apps/web/components/console/MobileConsoleNav.tsx`

All console components are Client Components — use `useTranslations("console")`.

- [ ] **Step 1: Migrate ActivityBar**

In `apps/web/components/console/ActivityBar.tsx`:

```tsx
import { useTranslations } from "next-intl";
import { Link } from "@/i18n/navigation";
// Remove: import Link from "next/link";

// Inside component:
const t = useTranslations("console");

// Replace TOP_ITEMS labels:
const TOP_ITEMS = [
  { href: "/app", key: "nav.dashboard", icon: "dashboard" },
  { href: "/app/projects", key: "nav.projects", icon: "projects" },
  { href: "/app/datasets", key: "nav.datasets", icon: "datasets" },
  { href: "/app/train", key: "nav.train", icon: "train" },
  { href: "/app/models", key: "nav.models", icon: "models" },
  { href: "/app/eval", key: "nav.eval", icon: "eval" },
];

// In render, use t(item.key) instead of item.label
// Same for BOTTOM_ITEMS — replace "(coming soon)" with t("nav.comingSoon")
```

- [ ] **Step 2: Migrate TopBar**

In `apps/web/components/console/TopBar.tsx`:

```tsx
import { useTranslations } from "next-intl";
import { Link } from "@/i18n/navigation";
import { LanguageSwitcher } from "@/components/LanguageSwitcher";
// Remove: import Link from "next/link";

// Inside TopBar:
const t = useTranslations("console");

// Replace SEGMENT_LABELS with t("breadcrumb.{seg}") calls in BreadcrumbNav
// Replace "Select project" with t("topbar.selectProject")
// Replace "Settings" menu item with t("topbar.settings")
// Replace "Back to site" with t("topbar.backToSite")
// Replace "QIHANG" brand text with t("brand") — or use common: t from common namespace
// Add LanguageSwitcher next to UserMenu in topbar-right
```

For the brand text in TopBar, use the `console` namespace `brand` key which renders "铭润科技控制台" / "Mingrun Tech Console". But the header only shows the short brand name. Two options:

Option A: Use `common` namespace in TopBar too. Since TopBar is a client component and needs both namespaces, use two hooks:
```tsx
const tc = useTranslations("common");
const t = useTranslations("console");
// Brand: tc("brand.company")
```

Option B: Add a short brand key to console.json. Simpler — go with this:
The TopBar brand already shows just the company name, so use `tc("brand.company")` from common.

- [ ] **Step 3: Migrate StatusBar**

In `apps/web/components/console/StatusBar.tsx`:

```tsx
import { useTranslations } from "next-intl";

// Inside:
const t = useTranslations("console");

// Replace "No project" with t("statusbar.noProject")
// Replace "API Connected" title with t("statusbar.apiConnected")
```

- [ ] **Step 4: Migrate CommandPalette**

In `apps/web/components/console/CommandPalette.tsx`:

```tsx
import { useTranslations } from "next-intl";
import { useRouter } from "@/i18n/navigation";
// Remove: import { useRouter } from "next/navigation";

const t = useTranslations("console");

// Replace placeholder with t("cmd.placeholder")
// Replace "No results found." with t("cmd.empty")
// Replace "Navigate" heading with t("cmd.navigate")
// Replace "Actions" heading with t("cmd.actions")
// Replace NAVIGATION_ITEMS labels with t("nav.{key}") calls
// Replace ACTION_ITEMS labels with t("cmd.newProject"), etc.
```

- [ ] **Step 5: Migrate MobileConsoleNav**

In `apps/web/components/console/MobileConsoleNav.tsx`:

```tsx
import { useTranslations } from "next-intl";
import { Link } from "@/i18n/navigation";
// Remove: import Link from "next/link";

const t = useTranslations("console");

// Replace "QIHANG Console" with t("brand")
// Replace NAV_ITEMS with translated labels + meta:
const NAV_ITEMS = [
  { href: "/app", key: "nav.dashboard", metaKey: "mobile.dashboard.meta" },
  // ...
];
// In render: t(item.key), t(item.metaKey)
// Replace "Back to site" with t("nav.backToSite")
```

- [ ] **Step 6: Commit**

```bash
git add apps/web/components/console/
git commit -m "feat(i18n): migrate all console shared components to translations"
```

---

### Task 10: Migrate all console pages

**Files:**
- Modify: All files under `apps/web/app/[locale]/(console)/app/`

All console pages follow the same pattern. Each page has a kicker, title, and description that come from its console-* namespace.

- [ ] **Step 1: Migrate console pages with page headers**

For each console page that has hardcoded text (billing, devices, settings, projects/[id]):

Pattern:
```tsx
import { getTranslations } from "next-intl/server";

export async function generateMetadata() {
  const t = await getTranslations("console-billing");
  return { title: `${t("title")} - ${(await getTranslations("common"))("brand.company")}` };
}

export default async function BillingPage() {
  const t = await getTranslations("console-billing");
  // Replace all hardcoded strings with t("kicker"), t("title"), t("description"), etc.
}
```

Apply to:
- `app/billing/page.tsx` — use `console-billing` namespace
- `app/devices/page.tsx` — use `console-devices` namespace
- `app/settings/page.tsx` — use `console-settings` namespace
- `app/projects/[id]/page.tsx` — use `console-projects` namespace (detail keys)
- `app/projects/page.tsx` — use `console-projects` namespace
- `app/datasets/page.tsx` — use `console-datasets` namespace
- `app/train/page.tsx` — use `console-train` namespace
- `app/models/page.tsx` — use `console-models` namespace
- `app/eval/page.tsx` — use `console-eval` namespace

- [ ] **Step 2: Replace `next/link` imports with locale-aware Link**

In all console pages that use `Link` from `next/link`, replace with:
```tsx
import { Link } from "@/i18n/navigation";
```

Check with:
```bash
grep -r "from \"next/link\"" apps/web/app/\[locale\]/\(console\)/ --include="*.tsx"
```

- [ ] **Step 3: Replace `useRouter` / `usePathname` from `next/navigation`**

In all console pages that use router from `next/navigation`:
```tsx
// Before
import { useRouter } from "next/navigation";
// After
import { useRouter } from "@/i18n/navigation";
```

- [ ] **Step 4: Commit**

```bash
git add apps/web/app/\[locale\]/\(console\)/
git commit -m "feat(i18n): migrate all console pages to translations"
```

---

### Task 11: Migrate auth pages and not-found

**Files:**
- Modify: `apps/web/app/[locale]/(auth)/login/page.tsx`
- Modify: `apps/web/app/[locale]/(auth)/register/page.tsx`
- Modify: `apps/web/app/[locale]/not-found.tsx`

- [ ] **Step 1: Migrate login page**

`apps/web/app/[locale]/(auth)/login/page.tsx`:

```tsx
"use client";

import { useTranslations } from "next-intl";
import { useRouter } from "@/i18n/navigation";
import { Link } from "@/i18n/navigation";
// Remove: import Link from "next/link";
// Remove: import { useRouter } from "next/navigation";

export default function LoginPage() {
  const t = useTranslations("auth");
  const router = useRouter();
  // ...

  return (
    <div className="auth-shell" ref={shellRef}>
      <div className="auth-card">
        <WorkbenchHero
          eyebrow={t("login.eyebrow")}
          title={t("login.heroTitle")}
          summary={t("login.heroSummary")}
          statusLabel={t("login.studioAccess")}
          points={[
            { label: t("login.point0.label"), detail: t("login.point0.detail") },
            { label: t("login.point1.label"), detail: t("login.point1.detail") },
            { label: t("login.point2.label"), detail: t("login.point2.detail") },
          ]}
        />
        {/* Form fields use t("login.email.label"), t("login.email.placeholder"), etc. */}
        {/* Error: t("login.error") */}
        {/* "还没有账号？" → t("login.noAccount") */}
        {/* "去注册" → t("login.register") */}
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Migrate register page**

Same pattern using `t("register.*")` keys from `auth` namespace.

- [ ] **Step 3: Migrate not-found page**

`apps/web/app/[locale]/not-found.tsx`:

```tsx
import { getTranslations } from "next-intl/server";
import { Link } from "@/i18n/navigation";

export default async function NotFound() {
  const t = await getTranslations("error");
  return (
    <div className="site-container py-10">
      <section className="public-hero glass-panel">
        <div className="public-hero-copy">
          <div className="site-kicker mx-auto w-fit">{t("notFound.kicker")}</div>
          <h1 className="site-title">{t("notFound.title")}</h1>
          <p className="site-lead mx-auto">{t("notFound.body")}</p>
          <div className="site-actions justify-center">
            <Link className="site-button" href="/">{t("notFound.home")}</Link>
            <Link className="site-button-secondary" href="/demo">{t("notFound.demo")}</Link>
          </div>
        </div>
      </section>
    </div>
  );
}
```

- [ ] **Step 4: Commit**

```bash
git add apps/web/app/\[locale\]/\(auth\)/ apps/web/app/\[locale\]/not-found.tsx
git commit -m "feat(i18n): migrate auth pages and not-found to translations"
```

---

### Task 12: Comprehensive next/link and next/navigation migration

**Files:**
- All files importing `next/link` or `next/navigation`

- [ ] **Step 1: Find all remaining `next/link` imports**

```bash
grep -r "from \"next/link\"" apps/web/ --include="*.ts" --include="*.tsx" -l | grep -v node_modules
```

Replace every occurrence with:
```tsx
import { Link } from "@/i18n/navigation";
```

Key files likely missed by earlier tasks:
- `apps/web/components/AppShell.tsx`
- `apps/web/components/MagneticButton.tsx`
- `apps/web/app/[locale]/(console)/app/page.tsx` (dashboard)
- Any detail pages (`models/[id]`, `datasets/[id]`, `train/[id]`)

- [ ] **Step 2: Find all remaining `next/navigation` imports**

```bash
grep -r "from \"next/navigation\"" apps/web/ --include="*.ts" --include="*.tsx" -l | grep -v node_modules
```

Replace `useRouter`, `usePathname` imports with `@/i18n/navigation` equivalents. `useSearchParams` stays with `next/navigation` (not provided by next-intl).

Key files:
- `apps/web/app/[locale]/(console)/app/models/[id]/page.tsx`
- `apps/web/app/[locale]/(console)/app/datasets/[id]/page.tsx`
- `apps/web/app/[locale]/(console)/app/train/[id]/page.tsx`
- `apps/web/components/AppShell.tsx`

Note: `notFound()` from `next/navigation` is NOT replaced — that's a Next.js core function, not a routing helper.

- [ ] **Step 3: Commit**

```bash
git add apps/web/
git commit -m "feat(i18n): complete next/link and next/navigation migration to locale-aware imports"
```

---

### Task 13: SEO — hrefLang and alternates

**Files:**
- Modify: `apps/web/app/[locale]/layout.tsx`
- Modify: All page files with `generateMetadata`

- [ ] **Step 1: Add alternates to locale layout metadata**

In `apps/web/app/[locale]/layout.tsx`, extend `generateMetadata`:

```tsx
import { getLocale } from "next-intl/server";

export async function generateMetadata(): Promise<Metadata> {
  const locale = await getLocale();
  const t = await getTranslations("common");
  const th = await getTranslations("home");
  return {
    title: {
      template: `%s - ${t("brand.company")}`,
      default: th("meta.title"),
    },
    description: th("meta.description"),
    openGraph: {
      locale: locale === "zh" ? "zh_CN" : "en_US",
    },
    alternates: {
      languages: {
        zh: "/",
        en: "/en",
      },
    },
  };
}
```

- [ ] **Step 2: Add page-specific alternates to each page's generateMetadata**

For each public page that defines `generateMetadata`, include alternates. Example for product page:

```tsx
export async function generateMetadata() {
  const t = await getTranslations("product");
  return {
    title: t("meta.title"),
    alternates: {
      languages: {
        zh: "/product",
        en: "/en/product",
      },
    },
  };
}
```

Apply same pattern to: ecosystem, demo, pricing, support, updates, login, register.

- [ ] **Step 3: Commit**

```bash
git add apps/web/app/
git commit -m "feat(i18n): add hrefLang alternates and og:locale for SEO"
```

---

### Task 14: Rename API route and final brand cleanup

Also rename the `QIHANG_EARBUD_SPEC` export (not just the endpoint constant).

**Files:**
- Move: `apps/web/app/api/qihang-earbud-spec/` → `apps/web/app/api/earbud-spec/`
- Modify: `apps/web/lib/qihang-earbud-spec.ts` (update endpoint constant)
- Global: Search and replace remaining "QIHANG" / "qihang" references

- [ ] **Step 1: Rename API route**

```bash
cd /Users/dog/Desktop/铭润/apps/web/app/api
git mv qihang-earbud-spec earbud-spec
```

- [ ] **Step 2: Update endpoint constant**

In `apps/web/lib/qihang-earbud-spec.ts`, rename both exports:
```ts
// Before
export const QIHANG_EARBUD_SPEC_ENDPOINT = "/api/qihang-earbud-spec";
export const QIHANG_EARBUD_SPEC = { ... };
// After
export const EARBUD_SPEC_ENDPOINT = "/api/earbud-spec";
export const EARBUD_SPEC = { ... };
```

Update all imports referencing the old constant names:
```bash
grep -r "QIHANG_EARBUD_SPEC" apps/web/ --include="*.ts" --include="*.tsx" -l | grep -v node_modules
```

- [ ] **Step 3: Audit remaining QIHANG references**

```bash
grep -ri "qihang" apps/web/ --include="*.ts" --include="*.tsx" --include="*.json" -l
```

Fix any remaining references:
- `qihang-viewer-contract.ts` — file name and internal references are technical, keep as-is (internal contract, not user-facing)
- `qihang:viewer-suspend` custom event — keep as-is (internal event name)
- Any user-facing "QIHANG" text should be replaced with `t("brand.company")` or hardcoded "铭润科技"/"Mingrun Tech"

- [ ] **Step 4: Add locale-aware redirects to next.config.js**

The existing redirects (`/how-it-works` → `/product`, etc.) only match the bare path. Add English variants:

```js
async redirects() {
  return [
    { source: "/how-it-works", destination: "/product", permanent: true },
    { source: "/en/how-it-works", destination: "/en/product", permanent: true },
    { source: "/docs", destination: "/support", permanent: true },
    { source: "/en/docs", destination: "/en/support", permanent: true },
    { source: "/contact", destination: "/support#contact", permanent: true },
    { source: "/en/contact", destination: "/en/support#contact", permanent: true },
  ];
},
```

- [ ] **Step 5: Update email references**

Replace `hello@qihang.ai` with `hello@mingrun-tech.com` in all translation files and any remaining hardcoded references.

- [ ] **Step 6: Commit**

```bash
git add apps/web/
git commit -m "feat(i18n): rename API route, complete QIHANG → 铭润科技 brand migration"
```

---

## Chunk 4: Testing & Verification

### Task 15: Update tests and verify

**Files:**
- Modify: `apps/web/tests/console-shell.spec.ts`
- Modify: `apps/web/tests/public-navigation-compat.spec.ts` (if exists)
- Test: TypeScript compilation

- [ ] **Step 1: Update console-shell.spec.ts**

The existing test checks for `data-theme="console"` and class `dark` — these are unchanged. But navigation test may need updating since URLs may have changed.

Review each test:
- `applies dark theme attributes` — page.goto("/app") should still work (zh default, no prefix)
- `ActivityBar visible on desktop` — unchanged
- `navigation works via ActivityBar` — the aria-label changed from "Datasets" to translated value. Update selector:

```ts
// Before
await page.click(".activity-bar-item[aria-label='Datasets']");
// After — in zh default, label is now "数据集"
await page.click(".activity-bar-item[aria-label='数据集']");
```

- `Cmd+K opens command palette` — unchanged
- `console pages load without errors` — routes unchanged for zh default

- [ ] **Step 2: Add i18n-specific test**

Create `apps/web/tests/i18n.spec.ts`:

```ts
import { test, expect } from "@playwright/test";

test.describe("i18n", () => {
  test("default locale is zh with no URL prefix", async ({ page }) => {
    await page.goto("/");
    const html = page.locator("html");
    await expect(html).toHaveAttribute("lang", "zh");
    // Brand should show 铭润科技
    await expect(page.locator("header strong")).toContainText("铭润科技");
  });

  test("English locale via /en/ prefix", async ({ page }) => {
    await page.goto("/en/");
    const html = page.locator("html");
    await expect(html).toHaveAttribute("lang", "en");
    await expect(page.locator("header strong")).toContainText("Mingrun Tech");
  });

  test("language switcher toggles locale", async ({ page }) => {
    await page.goto("/");
    // Click language switcher (shows "EN" in zh mode)
    await page.click("button:has-text('EN')");
    await expect(page).toHaveURL(/\/en\//);
    await expect(page.locator("html")).toHaveAttribute("lang", "en");
  });

  test("zh public pages load without errors", async ({ page }) => {
    const routes = ["/", "/product", "/ecosystem", "/demo", "/pricing", "/support", "/updates"];
    for (const route of routes) {
      const response = await page.goto(route);
      expect(response?.status()).toBeLessThan(400);
    }
  });

  test("en public pages load without errors", async ({ page }) => {
    const routes = ["/en/", "/en/product", "/en/ecosystem", "/en/demo", "/en/pricing", "/en/support", "/en/updates"];
    for (const route of routes) {
      const response = await page.goto(route);
      expect(response?.status()).toBeLessThan(400);
    }
  });
});
```

- [ ] **Step 3: Run TypeScript compilation check**

```bash
cd /Users/dog/Desktop/铭润/apps/web && npx tsc --noEmit
```

Fix any type errors.

- [ ] **Step 4: Commit**

```bash
git add apps/web/tests/
git commit -m "test(i18n): update console tests, add i18n test suite"
```

---

### Task 16: Final verification

- [ ] **Step 1: Run full test suite**

```bash
cd /Users/dog/Desktop/铭润/apps/web && npx playwright test
```

- [ ] **Step 2: Manual check — start dev server and verify**

```bash
cd /Users/dog/Desktop/铭润 && ./scripts/dev.sh
```

Verify in browser:
- `localhost:3000/` → Chinese, brand "铭润科技", no QIHANG anywhere
- `localhost:3000/en/` → English, brand "Mingrun Tech"
- Language switcher toggles between zh and en
- Console at `localhost:3000/app` → all labels in Chinese
- Console at `localhost:3000/en/app` → all labels in English

- [ ] **Step 3: Final audit for hardcoded text**

```bash
grep -r "QIHANG" apps/web/ --include="*.tsx" --include="*.ts" -l | grep -v node_modules | grep -v qihang-viewer | grep -v qihang-earbud
```

Should return no results (except internal technical identifiers).

- [ ] **Step 4: Commit if any fixes needed**

```bash
git add apps/web/
git commit -m "fix(i18n): final cleanup and verification"
```
