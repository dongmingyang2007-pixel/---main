# UI Comprehensive Fix Plan

> **For agentic workers:** Use superpowers:subagent-driven-development to implement. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Resolve all identified UI issues in both the public site and the console.

**Architecture:** Extract shared hooks from duplicated scroll/viewer logic; migrate console state management to React Context; improve eval page UX; mark placeholder pages; add loading skeletons.

**Tech Stack:** React 18, Next.js 16, TypeScript, Tailwind CSS, @tanstack/react-query (already installed), zod (already installed)

---

## Chunk 1: Public Site — Extract Shared Hooks & Refactor

### Task 1: Create `useViewerBridge` hook

**Files:**
- Create: `apps/web/lib/useViewerBridge.ts`

Extracts all duplicated viewer communication logic from HomeScrollStory and PublicStoryExperience:
- clearHandshakeTimers, suspendViewer, postToViewer, startHandshake
- Message event listener (READY/STATE handling)
- Suspend event listener
- Patch signature tracking and sending

### Task 2: Create `useStoryScroll` hook

**Files:**
- Create: `apps/web/lib/useStoryScroll.ts`

Extracts all duplicated scroll measurement logic:
- clamp, sameProgressArray utilities
- Geometry caching (from HomeScrollStory's optimized approach)
- Scene progress calculation with 0.02 precision
- Active scene detection via center-weighted proximity
- requestAnimationFrame batching

### Task 3: Refactor HomeScrollStory

**Files:**
- Modify: `apps/web/components/HomeScrollStory.tsx`

Replace inline scroll logic and viewer communication with useStoryScroll + useViewerBridge hooks. Keep: STORY_SCENES data, buildViewerPatch, HomeStoryNarrative memo, render JSX.

### Task 4: Refactor PublicStoryExperience

**Files:**
- Modify: `apps/web/components/PublicStoryExperience.tsx`

Replace inline scroll logic and viewer communication with useStoryScroll + useViewerBridge hooks. Eliminate all duplicated code.

---

## Chunk 2: Console — State Management & UX Improvements

### Task 5: Create ProjectContext

**Files:**
- Create: `apps/web/lib/ProjectContext.tsx`
- Modify: `apps/web/lib/useProjectSelection.ts`
- Modify: `apps/web/app/(console)/layout.tsx`

Replace module-level cache with React Context. Wrap console layout with provider.

### Task 6: Improve Eval Page

**Files:**
- Modify: `apps/web/app/(console)/app/eval/page.tsx`

Add structured result display: summary cards for each model version, side-by-side metric comparison, and better loading/error/empty states.

### Task 7: Mark Devices & Billing as "Coming Soon"

**Files:**
- Modify: `apps/web/components/AppShell.tsx`

Add visual "即将上线" badge to Devices and Billing nav items.

### Task 8: Add Console Loading Skeletons

**Files:**
- Create: `apps/web/components/ConsoleSkeleton.tsx`
- Modify: `apps/web/app/(console)/app/page.tsx`
- Modify: `apps/web/app/(console)/app/datasets/page.tsx`
- Modify: `apps/web/app/(console)/app/models/page.tsx`
- Modify: `apps/web/app/(console)/app/train/page.tsx`
- Modify: `apps/web/app/(console)/app/projects/page.tsx`
- Modify: `apps/web/styles/globals.css`

Add skeleton pulse components for data loading states across all console pages that fetch data.
